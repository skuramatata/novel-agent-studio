# 审稿批次膨胀与待确认数量修复（2026-09-10）

交付版本：0.1.89+20260910.151431.562.73e7a2cdb238。

## 实际故障

《那个不曾存在的岛》的 revision 166 草稿有 266 个审稿段落。专项输入受 22000 软额度约束，当前章无法完整装入后进入两两组合，生成 50 批；第 9 批因维度层多出空 preserve 和 fix 字段耗尽纠错预算。已保存的 33 条问题记录包含 20 条 open、8 条 advisory 和 5 条历史 awaiting_author；当前 pendingReview 实际为空，不能把 33 当作作者待答数量。

## 改动

- 当前章能放入有效预算时，优先完整提供当前章并轮换历史材料。弹性输入最多 44000，仍遵守模型硬上限及 1600 纠错余量；不截断已选历史，不取消跨段检查。
- 专项维度只兼容空 preserve / fix 占位。有内容的错位字段仍拒绝，不丢掉模型提出的修订要求。
- 分批及专项/综合合并时，仅对同目标、同处置、相容保留证据且证据互为子集的发现去重；其他冲突继续保留。
- 完整复审替换当前待办，未再次确认的旧项保留为历史，不能算作已修复；未完成或中断的批次不清空待办。
- 反馈与列表标题分别显示待核对、可选建议和当前待回答数量，问题列表默认折叠。作者待答以 pendingReview 及其实际回答为准。
- 旧分批失败允许按新输入恢复；当前协议的耗尽预算及其他步骤仍保持限制。

## 验证及边界

在独立任务副本使用原 GLM 模型及 Coding Plan 入口完成专项审稿：50 批降至 3 批；3 次请求耗时分别为 103215、64829、45290 毫秒，总计约 214 秒，无格式纠错。返回 5 条发现（4 条阻断、1 条建议）；4 条阻断在裁决入口均有依据，无待作者回答项。部分发现仍涉及同一段不同引用，保守去重没有将它们武断折叠。

持久化结果恢复与原结果一致，新增调用 0。上述测量覆盖专项审稿及裁决入口，不是整章补丁、复核和采纳耗时；模型单次响应仍需 45–103 秒。

269 项自动化测试全部通过。完成 npm run pack；检查 app.asar 的 build-info 及六个相关运行时文件与工作区一致。独立 Electron 用原作品/任务副本验证可恢复、版本显示、25 项待核对 / 8 项建议 / 当前无待回答提示；任务 IPC 约 47–54 毫秒、投影约 44 KB。未用隔离验证结果覆盖正式正文或正式检查点。

本机测量、原始响应及桌面截图位于被 Git 忽略的 verification/review-latency/。
