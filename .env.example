# 复制为项目根目录的 .env，再填写自己使用的供应商密钥。
# 只使用其中一个供应商时，另一项可以留空。
# 桌面端与评测脚本默认读取此目录的 .env；密钥不会注入前端。

# GLM Coding Plan 中国区密钥与模型
ZAI_CODING_CN_API_KEY=
MAIN_MODEL=glm-5.2

# MiniMax 密钥；模型与接口地址在桌面端“模型连接”中设置
MINIMAX_API_KEY=
